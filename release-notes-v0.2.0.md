# v0.2.0 发布说明

## 🎉 重大更新：画图质量全面升级

本次更新聚焦于流程图生成质量的全方位提升，新增了质量检测、自动布局优化、智能连线路由三大核心能力。

---

## ✨ 新增功能

### 1. 画图质量检测器
- 6 类质量问题自动检测：节点重叠、连线交叉、连线穿节点、标签压节点、斜线连线、平行边重合
- 0-100 分质量评分系统（绿/黄/红三色等级）
- 详细问题清单，标注问题类型、严重程度和位置

### 2. 自动布局引擎
- 完整实现 Sugiyama 分层布局算法
- 虚节点（Dummy Nodes）处理长跨度边，大幅减少连线交叉
- 中位数（Median）排序 + 贪心交换双重优化
- 多次随机尝试取最优结果（15 次随机初始排序）
- 节点重叠自动修复

### 3. 智能连线路由
- 5 种连线路由模式：libavoid 障碍避让、正交连线、单折点、侧边连接、直线
- 平行边自动分散，避免重合
- 连线自动避开节点（libavoid 模式）

### 4. 3 个新 AI 工具
- `auto_layout_diagram`：一键自动布局（分层+正交+重叠修复）
- `validate_diagram_quality`：质量检测与评分
- `set_edge_routing`：切换连线路由模式

### 5. 画布布局优化工具栏
- 一键自动布局按钮
- 质量检测按钮（实时显示评分徽标）
- 连线路由模式切换（下拉选择）

---

## 🔧 优化改进

### 调度系统 Bug 修复（P0-P2 共 12 个）
- **P0（4个）**：链式调度 contextMessages 过时、waitingForUser 不设置、轮数不递增、跨会话状态污染
- **P1（5个）**：DISPATCH:done 不触发完成、失败重试被拦截、并发保护缺失、chainLevel 失效、系统指纹跨会话
- **P2（3个）**：死代码清理、AGENT_PRIORITY key 不一致、工具执行超时、流式 AbortController 真中断

### 界面布局优化
- 左右布局调换：左侧聊天（38%）+ 右侧画布（62%），更符合操作直觉
- 画布工具栏重构，新增布局优化面板

### Prompt 强化
- 画图铁律从 10 条扩展到 16 条
- 新增 6 条失败条件，明确不合格的情况
- 新增 AI 自检流程：画完自动质检 → 分数不够自动布局优化 → 还不行手动重画

---

## 📐 布局算法 v2 升级清单

| 优化项 | 作用 |
|--------|------|
| 虚节点（Dummy Nodes） | 长跨度边中间插入节点，每层只处理相邻边 |
| 中位数排序 | 替代 barycenter 均值，抗异常值更稳定 |
| 贪心交换 | 排序后做相邻交换后处理，确认局部最优 |
| 多次随机取优 | 15 个随机初始排序，选交叉最少的结果 |
| 提前收敛检测 | 连续3轮无改进停止，节省时间 |
| 精确交叉计数 | 量化评估，用于比较和选优 |

---

## 📦 文件变更

- 新增：`src/services/diagramQuality.ts` — 质量检测器
- 新增：`src/services/diagramLayout.ts` — 分层布局引擎
- 新增：`src/services/diagramRouting.ts` — 连线路由集成
- 修改：`src/services/orchestrator.ts` — 调度 Bug 修复 + Prompt 强化
- 修改：`src/services/toolExecutor.ts` — 新增3个工具实现
- 修改：`src/config/tools.ts` — 新增3个工具定义
- 修改：`src/components/canvas/DrawIoCanvas.tsx` — 画布工具栏优化
- 修改：`src/App.tsx` — 左右布局调换
