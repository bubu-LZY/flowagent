## v0.6.0 - 2026-09-11

### ✨ 新功能（调度层 AI 语义化改造）
- PM 调度决策 AI 语义化：新增 AI 调度决策器，理解项目经理"跳过设计直接画""再细化设计""重新评审""可以收尾"等真实意图，代码校验合法性后执行，固定顺序仅作兜底
- 评审结论结构化判定：优先采用 validate_diagram_quality 工具的结构化结果（needsFix），彻底摆脱"VERDICT: PASS/FAIL"关键词匹配；结论缺失时 AI 语义兜底
- 评审失败根因 AI 归因：按质检 issues 语义判定「设计问题 vs 布局问题」，精准调度对应智能体修改
- 停止命令 AI 语义识别：区分"业务流程中的结束"（如"从购买到结束的流程图"）与"停止指令"，修复正常需求被误判停止导致流程不启动
- 设计稿真实解析：parseDesignBlueprint 解析真实节点行/连线行数量，替代"是否写了 NODES/EDGES 字样"正则匹配
- 智能布局 AI 语义接入：自动布局前 AI 分析节点/连线业务含义，输出布局方向（TB/LR）、回环边、语义分组，指导布局引擎排版；失败时降级纯算法
- 新增轻量结构化 AI 调用基础设施 callAIJson：非流式一次性 JSON 输出，带 15 秒超时与容错解析，失败返回 null 走确定性兜底

### 🐛 Bug 修复
- 修复「执行代理只读不写导致流程静默终止」：执行代理仅调用 get_diagram_xml 等读操作而不画图时，此前躲过"光说不练"检测且后续无人调度、流程无声断掉；现在检测升级为「未调用画布写入工具即强制重试」
- 修复 executorDidRealWork 判定语义不一致：历史上任何工具调用（含读画布/计算器）都被当作"真画过图"，使"派评审员前必须画过图"的合法性校验形同虚设；统一为「调用过画布写入类工具」
- 统一画布写入工具判定常量 CANVAS_WRITE_TOOLS：三处硬编码工具列表收敛为一处，避免新增画布工具时漏改

### ⚡ 优化
- 停止意图判断增加 40 字长度短路：长消息（明显是任务描述）不再额外消耗一次 AI 判停调用，降低每条消息的响应延迟

## v0.5.5 - 2026-09-11

### ✨ 新功能
- 新增内置优秀布局模板库（纵向主流程 / 两列Z字 / 横向角色链），AI 画图前可查询并选型参照，减少横线穿节点、连线过长
- 新增「孤立节点」检测维度：识别无连线节点并提示 AI 判断是否属于流程内（流程内孤立禁止交付，图例/注释可保留）
- 质量门禁新增打回提示与次数限制：最多自动修复 2 轮，超限停止交付并提示查看历史快照

### 🐛 Bug 修复
- 修复侧边工具栏拖拽松手后仍跟随鼠标：改用 Pointer Capture API 正确捕获与释放
- 重写自动布局算法：新增回环边识别、分层优化、回环边靠右路由，解决自动布局后评分下降、横线穿节点、连线过长

### ⚡ 优化
- 设计助手 / 执行代理提示词内置布局原则与模板范例，画前选型，减少反复返工

## v0.5.4 - 2026-09-11

### 🐛 Bug 修复
- 修复 PM 显式 @某智能体（如 @执行代理）被状态机忽略：导致派活后无人接手、流程中断
- 修复质量门禁被 PM @用户 交付提前跳过：导致画布存在严重问题（连线穿节点等）仍被交付

### ✨ 新功能
- 质量评分规则升级：只要存在任一严重问题（error），评分直接封顶 59 分判不合格，杜绝"有严重问题还评优秀"
- 新增「连线过长」检测维度：连线曼哈顿长度远超节点自然距离或画布对角线时告警/报错
- 新增「布局松散 / 空间不均」检测维度：识别离群节点与空洞分布，捕捉"下方空旷还拖长线"

## v0.5.3 - 2026-09-11

### ✨ 新功能
- 聊天消息支持渲染 AI 返回的画布截图（评审员看图评审的 PNG），悬浮即可一键复制到剪贴板
- 质量检测报告从 alert 弹窗升级为可选中、可复制的友好弹窗，并支持「复制流程图」「复制报告」
- 画布工具栏新增「复制为图片」按钮，一键把流程图导出为 PNG 复制到剪贴板
- 版本历史快照支持预览：点击先查看内容，确认后再恢复，不必先恢复再撤销

### 🐛 Bug 修复
- 修复图片无法复制到剪贴板：主进程新增 clipboard 通道（写图片/文本），渲染进程统一走该通道
- 修复自动布局质量误判：质量检测算法改用「正交折线」近似真实路由，不再用「节点中心直线」误报连线穿节点/交叉，评分更贴近实际画面
- 修复布局算法 assignCoordinates 层宽度索引错位 bug：层有空洞时节点坐标错位

### ⚡ 优化
- 质量分数阈值从 80 调整为 70，减少过严误杀
- 质量报告返回全部问题清单，逐条给出具体节点/连线/位置，并附带精准修复指引；AI 改为局部修复而非整图重画，节省 token
- 日志内容不再截断，完整记录工具参数、调度、角色、时间

## v0.5.2 - 2026-09-11

### 🐛 Bug 修复
- 修复任务"直接终止"：移除链式调度深度防线，执行代理/评审员不再被静默跳过
- 修复评审不通过后流程卡死：被状态机明确调度的智能体允许本轮再次发言，并新增评审连续失败上限防死循环
- 修复 MCP export_diagram 格式枚举与实现不一致（去掉不支持的 pdf，改为 jpeg/jpg）
- 修复 MCP 未实现工具返回"模拟成功"误导客户端：改为明确返回失败
- 修复 MCP 画布工具返回值缺 sessionId，客户端无法在同一会话继续操作
- 修复 get_chat_messages 忽略 sessionId 参数：支持按会话查询消息
- 统一多处硬编码版本号（0.2.0/0.4.0/0.4.1 → 0.5.2）

## v0.5.1 - 2026-09-11

### 🐛 Bug 修复
- 修复 AI 调用卡死无响应：新增连接超时保护（初始 API 调用 60 秒超时）
- 新增总超时兜底（15 分钟），防止极端情况完全卡死
- 优化流式超时逻辑：明确"两次数据间隔超时"语义，避免误杀慢输出

## v0.5.0 - 2026-09-11

### ✨ 新功能
- MCP 支持局域网访问：可选择监听 0.0.0.0，允许同网络设备连接
- MCP IP 白名单：非本地 IP 必须在白名单中才能访问，支持单个 IP 和 CIDR 网段（如 192.168.1.0/24）
- MCP CORS Origin 白名单：可配置允许跨域访问的 Origin

### 🔒 安全加固
- 四重安全防护：Token 认证 + IP 白名单 + 速率限制 + CORS 限制
- 非白名单远程 IP 速率限制更严格（20 次/分钟 vs 本地 60 次/分钟）
- 开启局域网访问前二次确认，提醒添加白名单
- MCP 工具全部白名单制，无文件系统访问权限，只能操作流程图相关功能
- 所有 MCP 调用会话隔离，在后台独立会话中执行，不影响用户当前界面

## v0.4.1 - 2026-09-11

### 🐛 Bug 修复
- 修复调度再次卡住：移除 PM 的 DISPATCH: done/none 提前终止逻辑，代码状态机完全接管流程推进
- 修复 MCP 画布工具调用无权限：MCP 系统调用（mcp-system）绕过智能体权限校验，直接执行所有工具
- 修复 MCP export_diagram 工具缺失：新增内部 export_diagram 工具，支持 png/svg/jpeg/xml/drawio 导出
- 修复 MCP 调用参数污染：剥离 sessionId/taskName 等 MCP 层参数后再传给内部工具

### ✨ 优化
- MCP 画布工具全部统一走 runCanvasTask → executeTool 流程，确保行为一致
- MCP Skill 文档更新：新增后台执行、任务面板、taskName 参数、IP 黑名单等说明
- 所有画布工具新增 taskName 参数，用于 MCP 任务面板展示

## v0.4.0 - 2026-09-10

### ✨ 新功能
- 三种视图模式切换：默认模式（聊天+画板）/ 纯对话模式 / 纯画板模式，顶部滑动卡片切换
- MCP 远程调用任务面板：左侧任务列表 + 右侧画板产物预览，顶部「MCP 任务」按钮入口
- MCP 远程调用后台执行：自动创建独立会话，操作完成切回原会话，不影响用户当前使用

### 🐛 Bug 修复
- 修复 MCP 画布工具调用全部失败的问题（drawFlowchart/clear 等方法不存在）
- 所有 MCP 画布工具改为复用 toolExecutor 的统一实现，确保参数和行为一致
- 修复跨会话轮数叠加：切换会话后重置轮数、等待用户、停止状态
- 修复误检测 @用户：PM 的 think 标签里提到"用户"概念词被误匹配
- 修复未配置模型时仍返回假对话：移除 mockAIResponse，未配置模型直接报错
- 发送消息前检查模型配置：未配置时弹窗提示并引导用户打开设置

## v0.3.7 - 2026-09-10

### 🐛 Bug 修复
- 修复跨会话轮数叠加：切换会话后重置轮数、等待用户、停止状态（新会话从第1轮开始）
- 修复误检测 @用户：PM 的 think 标签里提到"用户"概念词被误匹配，导致调度提前暂停等待用户回复
- 修复未配置模型时仍返回假对话：移除 mockAIResponse，未配置模型直接报错

### ✨ 新功能
- 发送消息前检查模型配置：未配置时弹窗提示并引导用户打开设置

## v0.3.6 - 2026-09-10

### 🔧 架构重构：代码状态机完全接管调度流程

**核心变化：不再依赖任何智能体写 DISPATCH 行，AI 的输出只负责内容，调度完全由代码控制。**

之前的问题：调度完全依赖 AI 在回复末尾写 `DISPATCH: @xxx`，AI 经常不写或写错，导致流程中断。

现在的方案：`determineNextAction()` 状态机 100% 控制调度：

| 当前角色 | 代码检查项 | 下一步动作 |
|---------|-----------|-----------|
| 项目经理 | 谁还没干活 | 按 设计 → 执行 → 评审 顺序推进 |
| 设计助手 | 有没有 NODES+EDGES 设计稿 | 没有 → 强制重试（最多2次）→ 还不行就跳过设计直接画图 |
| 设计助手 | 设计稿合格 | 自动调度执行代理 |
| 执行代理 | 有没有调用画布工具 | 有 → 自动调度评审员 |
| 评审员 | VERDICT: PASS / FAIL | PASS → 回 PM 交付；FAIL → 回执行代理修改 |
| 小白 | 无需检查 | 自动回 PM |

防死循环保护：
- 设计助手重试上限 2 次
- 执行代理"光说不练"重试上限 2 次
- 轮数硬上限 3 轮
- 链深度限制 5 层

## v0.3.5 - 2026-09-10

### 🐛 Bug 修复
- 修复日志面板始终为空：orchestrator 中从未调用 addLog，导致没有任何日志记录
- 修复 PM 未写 DISPATCH 行时回退到全 @ 模式导致并行调度混乱 → 代码自动推断下一个角色（严格串行）
- 修复设计助手不输出 NODES/EDGES 设计稿就调度执行代理 → 代码拦截并强制要求重写规范格式
- 执行代理"光说不练"重试次数从 1 次增加到 2 次，加强制提示指令
- 修复优先级顺序：执行代理移到评审员之前（先画图再评审）

### ✨ 新功能
- 新增 7 个关键节点日志记录：用户消息、调度开始、回复完成、DISPATCH 分发、兜底调度、工具失败重试、设计稿拦截
- 日志类型：消息、调度、分发、工具调用、工具结果、Skill、系统、错误

## v0.3.4 - 2026-09-10

### 🐛 Bug 修复
- 修复调度链断裂：执行代理/评审员/小白缺少 DISPATCH 行，完成后不知道交给谁
- 修复调度优先级顺序错误：执行代理应在评审员之前（先画图再评审）
- 修复 PM @ 不存在的角色：提示词中明确列出当前团队成员，禁止 @ 已移除的架构师/文档员

### ✨ 新功能
- 会话标题栏新增日志按钮（📋），点击打开详细日志面板
- 日志面板支持一键复制全部日志（纯文本格式，含时间/类型/角色/详情）
- 日志类型：消息、调度、分发、工具调用、工具结果、Skill、系统、错误

## v0.3.3 - 2026-09-10

### 🐛 Bug 修复
- 修复打包后 mcp-server.cjs 无法加载：require 路径需写完整扩展名（`.cjs` 不在 Node.js 默认解析列表中）

## v0.3.2 - 2026-09-10

### 🐛 Bug 修复
- 修复打包后 mcp-server.js 缺失导致启动失败（vite 构建脚本未复制新增的主进程文件）

## v0.3.1 - 2026-09-10

### 🐛 Bug 修复
- 修复主进程启动报错：`catch (e: any)` TypeScript 语法导致打包后 JS 运行失败

## v0.3.0 - 2026-09-10

### ✨ 新功能
- 新增版本自动检测更新：启动自动检查 + 每6小时检测 + 手动立即检查
- 新增更新弹窗：版本对比、更新说明展示、一键跳转下载、今日跳过
- 关于页面动态显示版本号，支持手动检查更新
- 画布独立窗口模式：主窗口自动卸载 iframe 释放内存（约 200-500MB）
- 生产环境禁用 DevTools（三层防护：配置 + 调试器拦截 + 右键禁用）
- 新增 IP 拉黑机制：认证失败 5 次或速率超限 10 次拉黑 24 小时，支持永久拉黑和解封

### 🐛 Bug 修复
- 修复"当前作业人"叠加显示的问题（智能体完成后未清除 streaming 状态）
- 修复"跳到最新"按钮随消息滚动且与背景文字色重合的问题
- 修复调度链断裂根因：activeAgents 空数组导致没人工作
- 修复 DISPATCH: done 误触发任务完成的问题
- 修复智能体失败重试逻辑和并发保护

### ⚡ 性能优化
- 消息列表添加 content-visibility，离屏消息跳过渲染
- 会话数据迁移到文件系统存储，解决 localStorage 5MB 上限问题
- 优化长会话 DOM 渲染性能

### 🔒 安全加固
- Token 时序安全比较（crypto.timingSafeEqual），防止时序攻击
- CORS 配置收紧，限制 MCP 服务跨域访问
- MCP 请求参数大小限制
- 生产环境全面禁用开发者工具
